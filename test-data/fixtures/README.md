# Tracked fixtures

Place only small, deterministic CSV compatibility fixtures in this directory. Each fixture should have a focused test that explains the behavior it covers. Large generated and transient files belong in `../generated/` and must not be committed.
